{
  "name": "@lane1/shared-types",
  "private": true,
  "version": "0.1.0",
  "description": "Gemeinsame DTOs/Zod-Schemas (User, SyncEvent, …), von apps/web und apps/api genutzt.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
